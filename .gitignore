# Machine-specific config — paths differ between soc006/exxact, never commit
config/paths.yaml

# Pipeline outputs — regenerated by runs, not source
stages/*/outputs/

# Raw datasets — far too large for git, and machine-specific paths anyway
data/
*.bin
*.pcd

# Python
__pycache__/
*.pyc

# OS/editor noise
*.swp

# opencode local session state, if any gets written into the repo dir
.opencode/
